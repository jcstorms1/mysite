import React from "react";
import { withRouter } from "react-router-dom";

const Resume = props => {
  return (
    <div>
      {/* <object id="resume" data={require("../files/Resume.pdf")} /> */}
    </div>
  );
};

export default withRouter(Resume);
